import React from 'react';
import CustomInputs from '../components/CustomInputs'
import NavButton from '../components/NavButton'

const Suggest = () => {
  return (
    <div>
      <NavButton message={'map'} />
      <CustomInputs />
    </div>
  );
}

export default Suggest
